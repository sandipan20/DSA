// WAP that check that the two no entered by user are equal or not 
#include<iostream>
using namespace std;
int main ()
{
    int a,b;
    cout<<"the two nos are respectively\n";
    cin>>a>>b;
    if(a==b)
    {cout<<"the no are equal";}
    else{cout<<"the no are not equal";}
    return 0;
}