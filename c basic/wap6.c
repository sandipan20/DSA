// WAP that check that the two no entered by user are equal or not 

# include<stdio.h>
int main()
{
    int a,b;
    printf("the nos.are ");
    scanf("%d%d",&a,&b);
    if(a==b)
    {printf("the nos are equal ");}
    else
    {printf("the nos are not equal ");}
    return 0;
}
