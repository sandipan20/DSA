// WAP that check wether the given no is even or odd

#include<stdio.h>
int main()
{
    int a;
    printf("the no is ");
    scanf("%d",&a);
    if(a%2==0)
    {printf("the no is even");}
    else{printf("the no is odd");}
    return 0;
}